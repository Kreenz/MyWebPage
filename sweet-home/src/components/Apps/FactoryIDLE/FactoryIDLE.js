import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
`

function FactoryIDLE(props) {

  return (
    <Wrapper>
    </Wrapper>
  );
}

export default FactoryIDLE;
